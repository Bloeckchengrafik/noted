[package]
name = "noted"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
# UI
bevy = { version = "0.9.1", features = ["dynamic"] }

# Stylus support on windows
windows = "0.44.0"

# Utilities
lazy_static = "1.4.0"